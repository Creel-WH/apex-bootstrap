# 2026-06-30 f299 APP_CODE 调整为 FMP

- Original Request: 将 `f299` 的 `APP_CODE` 从当前值调整为 `FMP`。
- AI Summary: 修正 `f299` 应用级 `APP_CODE` 计算值，避免登录链路、日志上下文和页面透传继续使用遗留编码 `JA_SALES_TOOL_SET`。

## 边界

- Env Alias: `file-management-platform@test`
- APP_ID: `299`
- App Code: `file-management-platform`
- Impacted Pages:
  - `P0`
  - `P9999`
- Impacted DB Objects:
  - `FMP_APEX_LOGIN`
  - `FMP_LOGIN_SUCCESS`
- Impacted APEX Shared Components:
  - application computation `APP_CODE`
  - application item `APP_CODE`

## 当前已知事实

- `APP_CODE` 的真相源是应用 computation，定义在 `apex/f299/application/shared_components/logic/application_computations/app_code.sql`。
- 当前 computation 值写死为 `JA_SALES_TOOL_SET`。
- 登录函数、登录后过程和大量页面日志都直接读取 `V('APP_CODE')` 或 `:APP_CODE`。

## 验收目标

1. 新会话登录后，应用程序项 `APP_CODE` 的值变为 `FMP`。
2. `P0_APP_CODE` 等页面透传值同步变为 `FMP`。
3. 至少完成一次数据库侧验证和一次真实登录后的运行时验证。
## Implementation

- 将 application computation `APP_CODE` 的静态值从 `JA_SALES_TOOL_SET` 调整为 `FMP`。
- 不改动登录函数与登录后过程，只修正它们读取的会话来源值。

## Validation Plan

1. 导入当前 `f299` APEX export 到 `file-management-platform@test/299`。
2. 用 SQL 确认 APEX metadata 中 `APP_CODE` computation 已变为 `FMP`。
3. 用真实浏览器登录 `JA016181`，确认登录成功后页面会话项 `P0_APP_CODE` 为 `FMP`。
## Validation Result

- 2026-06-30 已通过 `tools/apex_ai.sh import-app file-management-platform@test 299 299` 将当前 export 导入测试环境。
- SQL 验证结果为 `APP_CODE|FMP`，说明 APEX metadata 中的 application computation 已切换成功。
- 真实浏览器以 `JA016181 / lwh123..` 登录后，页面会话项 `P0_APP_CODE` 实际值为 `FMP`。
- 浏览器证据保存在 `output/playwright/f299-app-code-fmp/result.json` 与 `output/playwright/f299-app-code-fmp/after-login.png`。
