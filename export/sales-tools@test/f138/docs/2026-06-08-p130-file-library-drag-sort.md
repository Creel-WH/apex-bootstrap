# 2026-06-08 P130 文件库列表拖拽排序

## 基本信息
- 环境: `sales-tools@test`
- 应用: `f138`
- 页面: `P130`
- 参考页面: `P133`
- 状态: `ANALYZING`

## 业务目标
- 参考 `P133` 现有拖拽排序能力，为 `P130` 文件库列表增加同类拖拽排序。
- 用户可以在文件库列表中通过拖拽调整同级文件/文件夹顺序，并持久化到 `STS_FILE.SORT_NUM`。

## 验收标准
- `P130` 列表出现可见的拖拽排序入口。
- 仅从拖拽入口启动排序，不干扰列表现有点击、选择和操作行为。
- 拖拽时有明确的拖拽中/放置目标视觉反馈。
- 放手后顺序持久化成功，并刷新列表展示最新顺序。
- 同级文件/文件夹展示顺序与 `P133` 一致，按 `NVL(SORT_NUM, 999999), FILE_ID` 生效。
- 不因排序动作错误更新无关业务字段。

## 当前分析
- `P133` 已存在前端拖拽脚本、拖拽样式和 on-demand 进程 `SAVE_FILE_CHILD_ORDER`。
- `P130` 当前页面文档缺失，若本次变更涉及页面行为，需补建/更新 `docs/pages/P130.md`。
- 待确认 `P130` 当前列表区域、数据源、排序逻辑与 `P133` 的可复用差异。

## 预估影响范围
- `export/sales-tools@test/f138/apex/f138/application/pages/page_00130.sql`
- `export/sales-tools@test/f138/docs/pages/P130.md`（当前缺失，需补建）
- 可能复用但暂未确认是否需要改动的数据库对象: `STS_FILE`

## 验证思路
1. 结构检查 `P130` 页面脚本、样式、on-demand 进程与排序 SQL 是否就位。
2. 导入目标页面或应用后，在浏览器执行真实拖拽排序验证。
3. 核对页面文档与功能切片记录同步更新。
