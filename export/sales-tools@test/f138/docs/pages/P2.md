# P2 反馈管理-预览测试

## 2026-06-05 反馈内容显示恢复
- 列表查询中的反馈内容改为 `NVL(SF.FEEDBACK_CONTENT, SF.REMARK) AS FEEDBACK_CONTENT`。
- 目的：当历史反馈只更新了 `REMARK`、未补写 `FEEDBACK_CONTENT` 时，列表仍能显示反馈内容，不再空白。
- 当前已导入 `sales-tools@test/f138`。
