# 2026-06-17 切换 f299 权限相关页面的 STS_* 表引用到 FMP_*

## 背景

- f299 的相关业务表已迁移到 `FMP_*`。
- 当前 APEX 页面与授权方案中仍存在大量对 `STS_*` 的直接查询和 DML。
- 本次需求要求优先切换到 `FMP_*`，且**不修改页面业务逻辑**，只修改相关表的查询/写入目标地址。

## 目标

- 将 f299 在用权限相关页面中仍直接访问 `STS_*` 的 SQL，切换到对应 `FMP_*` 表。
- 保持页面行为、条件判断、流程分支、提交参数不变。
- 不处理无关测试页、废弃候选页，除非它们处在当前确认范围内。

## 当前初步范围

### 系统级权限链

- P221 权限管理
- P222 角色表单
- P223 成员管理
- P224 成员表单
- P225 切换角色
- shared components / authorizations

### 对象级权限链

- P101 分类管理
- P102 创建分类
- P103 自定义分类
- P104 自定义分类排序
- P120 文件库管理
- P121 文件库表单
- P133 文件库
- P139 文件夹及文件权限设置

## 初步受影响数据库对象

- `FMP_ROLE`
- `FMP_PERMISSION`
- `FMP_ROLE_PERMISSION`
- `FMP_SCOPE`
- `FMP_USER_ROLE`
- `FMP_USER`
- `FMP_FOLDER_CLASS`
- `FMP_FOLDER_LIB`
- `FMP_FOLDER`
- `FMP_FILE`
- `FMP_SYS_CONFIG`
- `FMP_OPERATION_LOG`

## 约束

- 只改表名/对象访问地址，不改业务逻辑。
- 先聚焦在用权限相关页面，不主动扩展到反馈、测试、登录等无关模块。
- 若发现仅有 `STS_*` 过程/函数封装而没有 `FMP_*` 对应封装，需要单独确认是否允许一起切换封装调用。

## 待确认

- 是否将本次范围严格收敛为“权限相关在用页面”，不包含明显测试页与废弃候选页。
- 若页面 SQL 里同时引用表与 `STS_*` 过程/函数，是否本次只切表，保留过程/函数调用不动。

## 本次实际变更范围

- `P101 / P102 / P103 / P104`
- `P120 / P121 / P133 / P139`
- `P221 / P222 / P223 / P224 / P225`
- `shared_components/security/authorizations/系统需求编辑权限_*`

## 本次实际切换内容

- 已将上述文件中能找到 `FMP_*` 对应物的 `STS_*` 表引用切换到 `FMP_*`
- 已将能找到 `FMP_*` 对应物的过程/函数调用切换到 `FMP_*`

## 本次保留项

- `page_00222.sql` 中仍保留 `sts_sync_permission_page_data(...)`
- 原因：当前仓库内未找到可确认对应的 `fmp_sync_permission_page_data(...)`
