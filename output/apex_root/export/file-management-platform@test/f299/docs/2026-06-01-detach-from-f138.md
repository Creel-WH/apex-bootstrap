# Detach f299 From f138

- Date: 2026-06-01
- Environment: `file-management-platform@test`
- App: `f299`
- Goal: remove residual application-level coupling to legacy app `f138` while preserving current page numbering, including page `138`

## Scope

- Update APEX split export metadata under `export/file-management-platform@test/f299/apex/f299/`
- Replace residual `p_default_application_id=>138` values with `p_default_application_id=>299`
- Keep page `138` and `P138_*` page item names unchanged because they refer to the current app's page structure, not the legacy app boundary

## Impacted Pages

- None as a functional page-behavior change
- Many page export files are touched only to correct application metadata header values

## Impacted DB Objects

- None

## Verification Plan

- Run repository environment check for `file-management-platform@test`
- Search for remaining `p_default_application_id=>138`, `f138`, and legacy environment references under `f299`

## Result

- Completed application-level detachment from legacy app `f138`
- Replaced residual `p_default_application_id=>138` headers in APEX split export with `299`
- Confirmed page `138` content remains in place and was only updated at the application metadata header level
